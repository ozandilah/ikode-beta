import { ReactElement } from 'react';
interface Props {
    color?: "light" | "dark";
    className?: string;
}
declare function Brand({ color, className }: Props): ReactElement;
export default Brand;
